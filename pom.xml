<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.idanalyzer</groupId>
    <artifactId>id-analyzer-v2</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>ID Analyzer API v2 SDK</name>
    <description>Java/Kotlin client library for the ID Analyzer API v2: worldwide passport,
        driver license and ID card scanning, biometric face/liveness verification, AML/PEP
        screening, DocuPass remote verification and e-signature, KYC profile management and
        contract generation. Targets the api2.idanalyzer.com fleet.</description>
    <url>https://github.com/idanalyzer/id-analyzer-v2-java</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>ID Analyzer</name>
            <email>support@idanalyzer.com</email>
            <organization>Evith Information Technology Ltd.</organization>
            <organizationUrl>https://www.idanalyzer.com</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/idanalyzer/id-analyzer-v2-java.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/idanalyzer/id-analyzer-v2-java.git</developerConnection>
        <url>https://github.com/idanalyzer/id-analyzer-v2-java</url>
    </scm>

    <properties>
        <maven.compiler.release>11</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jackson.version>2.17.2</jackson.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals><goal>jar-no-fork</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.7.0</version>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals><goal>jar</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
